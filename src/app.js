import {authorize,initializeProject,loadLocations,saveLocation} from "./google-api.js";
const locations=[];let map,editingId=null;const $=id=>document.getElementById(id),status=(m,t="")=>{$("status").textContent=m;$('status').className=`status ${t}`};
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function render(){const q=$("searchInput").value.toLowerCase();const rows=locations.filter(x=>[x.name,x.address,x.description,x.category].join(" ").toLowerCase().includes(q));$("listMeta").textContent=`${rows.length} 個地點`;$("locationList").replaceChildren(...rows.map(x=>{const b=document.createElement("button");b.className="location-card";b.innerHTML=`<strong>${esc(x.name)}</strong><small>${esc(x.address||`${x.latitude}, ${x.longitude}`)}</small>`;b.onclick=()=>{map.setView([x.latitude,x.longitude],15);openForm(x)};return b}))}
function openForm(x){editingId=x?.id||null;$("dialogTitle").textContent=x?"編輯地點":"新增地點";for(const [id,key] of [["name","name"],["address","address"],["description","description"],["category","category"],["latitude","latitude"],["longitude","longitude"]])$(id).value=x?.[key]??"";$('locationDialog').showModal()}
async function save(e){e.preventDefault();const v={id:editingId||crypto.randomUUID(),name:$("name").value.trim(),address:$("address").value.trim(),latitude:Number($("latitude").value||23.7),longitude:Number($("longitude").value||120.9),description:$("description").value.trim(),category:$("category").value.trim(),createdAt:editingId?(locations.find(x=>x.id===editingId)?.createdAt||new Date().toISOString()):new Date().toISOString(),updatedAt:new Date().toISOString()};if(!v.name)return $("formError").textContent="請填寫名稱。";try{await saveLocation(v);const i=locations.findIndex(x=>x.id===v.id);i<0?locations.push(v):locations[i]=v;render();$('locationDialog').close();status("地點已同步至 Google Sheets","success")}catch(error){status(`儲存失敗：${error.message}`,"error")}}
async function connect(){const b=$("authButton");b.disabled=true;b.textContent="連線中…";try{await authorize();await initializeProject();locations.push(...await loadLocations());render();$("addButton").disabled=false;$("fitButton").disabled=false;b.textContent="Google 環境：已準備";status("Google 環境：授權與能力驗證成功","success")}catch(e){status(`初始化失敗：${e.message}`,"error");b.disabled=false;b.textContent="初始化我的地圖"}}
map=L.map("map").setView([23.7,120.9],8);L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{attribution:"© OpenStreetMap 貢獻者"}).addTo(map);$("authButton").onclick=connect;$("addButton").onclick=()=>openForm();$("locationForm").onsubmit=save;$("searchInput").oninput=render;$("closeSidebar").onclick=()=>$('sidebar').classList.remove('is-open');$("fitButton").onclick=()=>locations.length&&map.fitBounds(L.latLngBounds(locations.map(x=>[x.latitude,x.longitude])),{padding:[40,40]});render();
